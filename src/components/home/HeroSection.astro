---
/**
 * Hero section for home page
 * Dark Doppler aesthetic with glowing stat tiles
 */

import { formatNumber, formatPercentage } from '../../utils/formatters';

interface GlobalStats {
  total_companies: number;
  total_questions: number;
  total_snippets: number;
  average_financial_rate_all: number;
}

interface Props {
  globalStats: GlobalStats;
}

const { globalStats } = Astro.props;
---

<section class="border-b border-slate-800 bg-slate-950/80">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-14">
    <!-- Title and subtitle -->
    <div class="text-center mb-12">
      <h1 class="text-4xl sm:text-[46px] font-semibold text-slate-50 mb-4 tracking-tight">
        Cultivating Transparency - Disclosure Explorer
      </h1>
      
    </div>

    <!-- Global stats bar -->
    <div class="grid grid-cols-2 md:grid-cols-3 gap-4 max-w-5xl mx-auto">
      <!-- Companies -->
      <div class="surface p-4 text-center">
        <div class="text-xs uppercase tracking-widest text-slate-400 mb-1.5">Companies</div>
        <div class="text-2xl sm:text-3xl font-semibold text-slate-100 mb-1.5">
          {globalStats.total_companies}
        </div>
        <div class="text-xs text-slate-400">with verified disclosure reviews</div>
      </div>

      <!-- Questions -->
      <div class="surface p-4 text-center">
        <div class="text-xs uppercase tracking-widest text-slate-400 mb-1.5">Risk Questions</div>
        <div class="text-2xl sm:text-3xl font-semibold text-slate-100 mb-1.5">
          18
        </div>
        <div class="text-xs text-slate-400">analyzed for transparency</div>
      </div>

      <!-- Disclosures -->
      <div class="surface p-4 text-center">
        <div class="text-xs uppercase tracking-widest text-slate-400 mb-1.5">Evidence Disclosures</div>
        <div class="text-2xl sm:text-3xl font-semibold text-slate-100 mb-1.5">
          {formatNumber(globalStats.total_snippets)}
        </div>
        <div class="text-xs text-slate-400">verified disclosures</div>
      </div>
    </div>
  </div>
</section>
